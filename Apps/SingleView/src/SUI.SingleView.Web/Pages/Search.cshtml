@page
@model SUI.SingleView.Web.Pages.Search


@{
    ViewBag.Title = Model.HasResults ? "Search results" : "Find a child's safeguarding record";
}

@if (Model.HasResults)
{
    <partial name="_SearchResults"/>
}
else
{
    <partial name="_SearchForm"/>
}