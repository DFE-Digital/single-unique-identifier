# Single Unique Identifier (SUI) Service — Data Architecture for LLM consumption

Note: This file has been generated by an LLM -- reviewed and corrected by a human.

> **Source**: [`data-architecture.drawio.png`](./data-architecture.drawio.png)
> **Layer**: Data Architecture (Data Flow Diagram)

## 1. Architecture Boundaries (Domains)

- **System provider**
  Providers of systems holding information relevant to provision of child
  social care. Can be a public sector agency maintaining an in-house system or a
  private sector supplier of a provided system.

- **DfE**
  Department for Education central matching, routing, and subscription orchestration services.

- **NHS England**
  External NHS digital health infrastructure providing patient identity and event messaging (PDS, MNS, MESH).


## 2. Component Inventory

### 2.1 External Entities

- **`Agency A, B, C, etc.`** (`System provider`)
  Local agencies using the system provider's database to store information relevant to child social care.

### 2.2 Data Stores

- **`[D1]` PDS Patients** (`NHS England`)
  Personal Demographics Service database containing authoritative patient identities.

- **`[D2]` MNS Subscriptions** (`NHS England`)
  Message Notification Service registry tracking active subscriptions for demographic/GP changes.

- **`[D3]` MESH Events Mailbox** (`NHS England`)
  Message Exchange for Social Care and Health mailbox holding pending patient change events.

- **`[D4]` Children** (`Supplier`)
  System provider database of child records across participating agencies.

### 2.3 Processes

- **`[P01]` Match children without an identifier to an NHS number** (`System provider`)
  Identifies unlinked local records and coordinates matching requests with DfE.

- **`[P02]` Match demographics to NHS number** (`DfE`)
  Core matching engine querying NHS PDS and establishing identity links.

- **`[P03]` Search for a patient** (`NHS England`)
  Executes search queries against PDS.

- **`[P04]` Subscribe for patient record changes** (`NHS England`)
  Registers interest in future updates for matched NHS numbers in MNS.

- **`[P05]` Record new patient record change events** (`NHS England`)
  Detects PDS mutations matching active subscriptions and posts change events.

- **`[P06]` Update child record, based on patient record change event** (`System provider`)
  Applies NHS number or GP practice updates back to the system provider child store.

- **`[P07]` Forward patient record change events** (`DfE`)
  Ingests events, acknowledges receipt, and relays relevant updates to system providers.

- **`[P08]` Read available events** (`NHS England`)
  Polls or consumes queued messages from the MESH mailbox.

- **`[P09]` Acknowledge events** (`NHS England`)
  Confirms receipt of messages to remove them from the MESH mailbox.

- **`[P10]` Clean duplicate subscriptions** (`DfE`)
  Identifies and reconciles duplicate or obsolete MNS subscriptions.

- **`[P11]` Read all subscriptions** (`NHS England`)
  Exports current active subscription records from MNS.

- **`[P12]` Delete subscriptions** (`NHS England`)
  Removes target subscription IDs from MNS.


## 3. End-to-End Operational Pipelines

### Pipeline 1: Child Ingestion, Demographic Matching & Change Subscription

Handles initial ingestion from local agencies, matching against PDS, returning NHS numbers, and establishing ongoing change subscriptions.

1. `Agency A, B, C, etc.` → `[D4] System provider Children`
   *Payload*: `Child demographics`
2. `[D4] System provider Children` → `[P01] Match children`
   *Payload*: `Child demographics`
3. `[P01] Match children` → `[P02] Match demographics`
   *Payload*: `Child demographics`
4. `[P02] Match demographics` → `[P03] Search for a patient`
   *Payload*: `Child demographics`
5. `[D1] PDS Patients` → `[P03] Search for a patient`
   *Payload*: `FHIR Patients`
6. `[P03] Search for a patient` → `[P02] Match demographics`
   *Payload*: `FHIR Searchset and Patient bundles`
7. `[P02] Match demographics` → `[P01] Match children`
   *Payload*: `NHS numbers and GP codes`
8. `[P01] Match children` → `[D4] System provider Children`
   *Payload*: `NHS numbers and GP codes`
9. `[P02] Match demographics` → `[P04] Subscribe for changes`
   *Payload*: `NHS Numbers`
10. `[P04] Subscribe for changes` → `[D2] MNS Subscriptions`
    *Payload*: `Subscriptions for NHS number and GP changes on a patient`


### Pipeline 2: Patient Record Change Ingestion & System provider Notification

Monitors demographic and GP changes published by NHS England and propagates them back into system provider records.

1. `[D1] PDS Patients` → `[P05] Record new events`
   *Payload*: `Patient record changes`
2. `[D2] MNS Subscriptions` → `[P05] Record new events`
   *Payload*: `Active patient record subscriptions`
3. `[P05] Record new events` → `[D3] MESH Events Mailbox`
   *Payload*: `Patient record change events`
4. `[D3] MESH Events Mailbox` → `[P08] Read available events`
   *Payload*: `Patient record change events`
5. `[P08] Read available events` → `[P07] Forward events`
   *Payload*: `Patient record change events`
6. `[P07] Forward events` → `[P09] Acknowledge events`
   *Payload*: `Event acknowledgements`
7. `[P09] Acknowledge events` → `[D3] MESH Events Mailbox`
   *Payload*: `Event acknowledgements`
8. `[P07] Forward events` → `[P06] Update child record`
   *Payload*: `NHS number or GP code change notifications`
9. `[P06] Update child record` → `[D4] System provider Children`
   *Payload*: `NHS numbers and GP codes`


### Pipeline 3: Subscription Hygiene & De-duplication

Periodic maintenance routine to detect duplicate or orphaned subscriptions and keep MNS tidy.

1. `[D2] MNS Subscriptions` → `[P11] Read all subscriptions`
   *Payload*: `Active patient record subscriptions`
2. `[P11] Read all subscriptions` → `[P10] Clean duplicate subscriptions`
   *Payload*: `Active patient record subscriptions`
3. `[P10] Clean duplicate subscriptions` → `[P12] Delete subscriptions`
   *Payload*: `Subscription IDs`
4. `[P12] Delete subscriptions` → `[D2] MNS Subscriptions`
   *Payload*: `Subscription IDs`


## 4. Complete Data Flow Matrix

- **`F01`** `Agency A, B, C, etc.` → `[D4] System provider Children` : *Child demographics*
- **`F02`** `[D4] System provider Children` → `[P01] Match children` : *Child demographics*
- **`F03`** `[P01] Match children` → `[P02] Match demographics` : *Child demographics*
- **`F04`** `[P02] Match demographics` → `[P03] Search for a patient` : *Child demographics*
- **`F05`** `[D1] PDS Patients` → `[P03] Search for a patient` : *FHIR Patients*
- **`F06`** `[P03] Search for a patient` → `[P02] Match demographics` : *FHIR Searchset and Patient bundles*
- **`F07`** `[P02] Match demographics` → `[P01] Match children` : *NHS numbers and GP codes*
- **`F08`** `[P01] Match children` → `[D4] System provider Children` : *NHS numbers and GP codes*
- **`F09`** `[P02] Match demographics` → `[P04] Subscribe for changes` : *NHS Numbers*
- **`F10`** `[P04] Subscribe for changes` → `[D2] MNS Subscriptions` : *Subscriptions for NHS number and GP changes on a patient*
- **`F11`** `[D1] PDS Patients` → `[P05] Record new events` : *Patient record changes*
- **`F12`** `[D2] MNS Subscriptions` → `[P05] Record new events` : *Active patient record subscriptions*
- **`F13`** `[P05] Record new events` → `[D3] MESH Events Mailbox` : *Patient record change events*
- **`F14`** `[D3] MESH Events Mailbox` → `[P08] Read available events` : *Patient record change events*
- **`F15`** `[P08] Read available events` → `[P07] Forward events` : *Patient record change events*
- **`F16`** `[P07] Forward events` → `[P09] Acknowledge events` : *Event acknowledgements*
- **`F17`** `[P09] Acknowledge events` → `[D3] MESH Events Mailbox` : *Event acknowledgements*
- **`F18`** `[P07] Forward events` → `[P06] Update child record` : *NHS number or GP code change notifications*
- **`F19`** `[P06] Update child record` → `[D4] System provider Children` : *NHS numbers and GP codes*
- **`F20`** `[D2] MNS Subscriptions` → `[P11] Read all subscriptions` : *Active patient record subscriptions*
- **`F21`** `[P11] Read all subscriptions` → `[P10] Clean duplicate subscriptions` : *Active patient record subscriptions*
- **`F22`** `[P10] Clean duplicate subscriptions` → `[P12] Delete subscriptions` : *Subscription IDs*
- **`F23`** `[P12] Delete subscriptions` → `[D2] MNS Subscriptions` : *Subscription IDs*
